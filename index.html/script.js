// For now, let's just test if JS is working
console.log("SkillSwap Hub is ready!");
// Get button from HTML
let startButton = document.querySelector("button");

// Add click event
startButton.addEventListener("click", function () {
    alert("Welcome to SkillSwap Hub! Let's find your skill match.");
});
